server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql:///mall?characterEncoding=utf8&useSSL=false
    username: root
    password: password
mybatis:
  mapper-locations:
    - classpath:mapper/*.xml
    - classpath*:com/**/mapper/*.xml
